# PSX AI Market Intelligence

Independent dashboard, deployable free on GitHub Pages.

- PSX-first architecture
- No Yahoo Finance
- Refresh button reloads `data/market.json`
- 2Y low/high fields intentionally remain blank until verified historical backfill is supplied
- Future authorized PSX/broker data can replace the seed JSON without rebuilding the UI
